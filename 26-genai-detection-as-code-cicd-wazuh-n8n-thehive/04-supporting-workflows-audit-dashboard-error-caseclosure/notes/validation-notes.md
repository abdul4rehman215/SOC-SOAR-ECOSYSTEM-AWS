# Supporting Workflow Validation Notes

Evidence captured or planned:

- dashboard collector received metric events from Flow A, Flow B, and Flow C
- dead-letter workflow can be tested with a temporary failing workflow
- Flow D case closure sync updates closure table, audit events, dashboard rows, and Slack notification
