# Shared Source Material

This folder contains shared scripts, schemas, mappings, metadata, tests, and config templates used across the capstone.

Important: live `.env.ci` and SSH private keys are not included. Use `config-templates/.env.ci.example` and map secrets through n8n credentials or a secure secret store.
