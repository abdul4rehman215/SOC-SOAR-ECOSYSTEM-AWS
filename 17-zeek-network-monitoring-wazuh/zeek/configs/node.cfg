# zeek/configs/node.cfg
# Purpose: ZeekControl node configuration (standalone deployment)
# NOTE: Replace interface with your actual NIC (check: ip a)

[zeek]
type=standalone
host=localhost
interface=ensX0
